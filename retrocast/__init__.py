# The Exhaust retrocast harness (SPEC-08) — the credibility engine. Generic and
# data-shape-agnostic: every index feeds it (entity, t, score) observations + (entity, event_t)
# labels. No metered LLM. See harness.py; per-index specs live in retrocast/<index>/.
