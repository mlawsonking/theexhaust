# indexes/

One directory per index, created by the Phase 3 workbook compiler and filled in Phase 4. Each contains: methodology doc (frozen version-controlled), retrocast spec + results, weekly bounded jobs with acceptance criteria, artifact templates, and the index's receipts store layout.

Empty until Phase 3. Do not hand-create index directories — they are compiled.
