# indexes/

One directory per index. The hand-written seed is `WORKBOOK.md` (authored at gate time); everything else is emitted by the workbook compiler per [ops/SPEC-07](../ops/SPEC-07-workbook-compiler.md): frozen methodology, retrocast spec + results, bounded weekly jobs with acceptance criteria, artifact templates, launch-gate draft.

Empty until Phase 4 (the compiler is BUILD-06; the first indexes are hand-built to the same spec at BUILD-03/04/05, then reconciled). Do not hand-create index directories outside that path.
