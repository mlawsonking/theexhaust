# The Exhaust shared engines. E1 Posting-Diff (ATS boards) is the first; the layoffs/wages/
# ghost-jobs family rides it. Deterministic cores carry the product; LLM semantics are an
# enhancement layer, batched and gated (never in R1). Stdlib-only.
