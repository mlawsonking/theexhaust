# WORKPLAN — the build-grind queue

*Maintained by the ORCHESTRATOR; consumed one item at a time by WORKER sessions via `NEXT.md` (see [`ops/BUILD-PROTOCOL.md`](../BUILD-PROTOCOL.md)). Items are session-sized. Work strictly top-down unless NEXT.md says otherwise. Every item: scope → read-list → build → accept → catches/fallbacks. Status vocab: `queued | next | in-progress | done(commit) | partial | blocked(reason)`.*

*Ordering doctrine: deploy-and-verify before new construction (the archive clock is the scarcest resource); flagship retrocast before launch surfaces; launch surfaces before expansion. Gameplan §6 acceptance criteria govern; this plan slices them into sessions.*

---

## Phase A — light the machine (immediately after operator BUILD-00 errands)

### W-000 · BUILD-00 acceptance gatecheck — `done` (2026-07-27, in commit at hand-off)
**Scope:** verify the five operator errands actually completed; wire nothing yet.
**Read:** `docs/03-GAMEPLAN.md` §6 BUILD-00 only; `ops/SPEC-02` §1.
**Do:** check git remote exists + push works; `ci` Action ran green on GitHub; R2 credentials present as Actions secrets AND usable (list bucket via boto3 locally with env creds); custom domain on the bucket serves an object; ntfy topics reachable (send a test to each; operator confirms phone receipt out-of-band); healthchecks project exists.
**Accept:** all five confirmed in the buildlog with evidence, or a precise per-item failure report.
**Catch:** any missing piece → report EXACTLY which Vikunja task (#9–13) is incomplete and in what way; STOP. Do not partially wire.
**Result (2026-07-27, precise stop):** **all five failed/unverifiable — none complete.** #9 repo: no git remote, `gh` confirms `theexhaust` does not exist on GitHub → no push, `ci` never ran there. #10 R2 bucket+custom domain: no creds, no bucket, untestable. #13 Actions secrets: unset locally + unverifiable (no repo). #12 ntfy: topic names not available to the session, no phone confirmation → not passed. #11 healthchecks: no ping URL/evidence. Suite re-run green (0 failures). NOTHING wired. Evidence in buildlog 2026-07-27. **`NEXT.md` stays W-000** — re-run this gatecheck once the operator reports #9–13 done; do NOT advance to W-001 until it passes.

### W-001 · R2 backend live + restore drill — `done` (2026-07-28, in commit at hand-off)
**Result:** 6 collectors stored real vintages to R2 (14 objects / 390 MB); `select_storage` moved to `framework.py` + `ats-boards` R2-routed (was hardcoded LocalFS) with a regression test; restore drill PASS through `archive.theexhaust.org` (sha256 + schema match, zst CSV & raw ZIP); `ci/run_all.py` added + CI switched to it; `BUDGET.json` at 0.39 GB / $0. Finding: Cloudflare Bot Fight Mode 403s bare `Python-urllib` UA (framework `DEFAULT_UA` unaffected) — WORKPLAN candidate for W-007. Evidence: buildlog 2026-07-28.
**Scope:** the fleet writes to real R2; prove restore.
**Read:** `collectors/framework.py`, `collectors/run.py`, `ops/SPEC-01` §3/§6.
**Do:** `pip install boto3` (and add to CI install); run each of the 6 verified collectors once against R2 (`--verify` off, env creds); restore drill per SPEC-01 §6 (pull yesterday's snapshot via the custom domain, revalidate schema, match manifest hash); update `BUDGET.json` storage figures; add `ci/run_all.py` (runs the §5 suite, exits nonzero on any failure) and switch CI to it.
**Accept:** 6 collectors stored-or-unchanged against R2; restore drill passes; suite green.
**Catches:** R2 auth fails → re-check secret names (SPEC-02 env contract) before touching code; a collector fails live → its per-collector quarantine/pause semantics are the fallback, never edit-and-hope; datacenter-403 → the SPEC-01 §4.5 ladder (log the switch if operator-box fallback used).

### W-002 · Actions cron fleet + the 367 MB complaints pull — `done` (2026-07-28, in commit at hand-off)
**Result:** reusable `_collector.yml` + 6 per-collector scheduled workflows (odd-minute, staggered, 2–4× over-scheduled per SPEC-01 §2 cadence, `workflow_dispatch`, per-collector concurrency, R2+`HC_*` env). All 6 dispatched **green, zero datacenter-403s**: nhtsa-recalls/nhtsa-complaints(367 MB/63 s)/ats-boards **stored** to R2 (objects verified present via boto3 list), cms/cpsc/fdic **dedupe'd `unchanged`**, `HC_NHTSA_RECALLS` heartbeat **pinged**; 2nd cms firing re-confirmed dedupe. `ats-boards` brought to the R1 job contract (heartbeat + nonzero-exit-on-quarantine) with a regression test. Suite 8/8. **HIGH-priority follow-up filed (state-commit-back, below).** Evidence: buildlog 2026-07-28. Commit `1e3b776`.
**Scope:** collectors run scheduled in R1 with cron-drift defenses; first full `nhtsa-complaints` vintage archived.
**Read:** `ops/SPEC-02` §1, `.github/workflows/ci.yml` as pattern, `collectors/nhtsa.py`.
**Do:** one workflow per collector (or grouped ≤3 where cadence matches): odd-minute schedules, 2–4× over-scheduling, `workflow_dispatch`, per-collector concurrency groups, heartbeat env wiring; a dedicated workflow runs `nhtsa-complaints` full pull (chunk under 6-hr cap; it's one file — fine); confirm dedupe logs on the second firing.
**Accept:** every enabled collector has ≥1 green scheduled run in Actions; complaints vintage (51-field, ~500k+ rows) in R2 with manifest; heartbeats pinged (visible in healthchecks).
**Catches:** complaints download flaky in Actions → retry once, else run from operator box at identical politeness and note it; cron doesn't fire (drift) → `workflow_dispatch` manually this session, the heartbeat grace windows are the systemic answer; row count wildly off layout → ZipTabSchema quarantines it — file the gate, don't "fix" the schema to make it pass.

### W-002b · Collector state-commit-back (dedupe persistence) — `done` (2026-07-28, in commit at hand-off)
**Result:** built the locked option (a). Per-collector `ops/state/health/<c>.json` (source of truth); `run.py`/`ats_boards.py` write them in R1; `report.merged_health()` merges per-collector (authoritative) + legacy fallback; `weekly.py` re-materializes the legacy view. `_collector.yml` gains `contents:write`+`fetch-depth:0`+state-commit step (skip-on-unchanged, rebase-retry ≤2 then loud-fail, `[skip ci]`); `keepalive.yml` monthly backstop; migrated 6 collectors; tests +2 (reader-merge, per-collector makedirs). **Caught+fixed a `startup_failure`:** repo default token perm is `read`, so the reusable's `contents:write` exceeded the callers' grant — added `permissions: contents: write` to the 6 callers (no repo-setting change). **Proven live:** firing 1 stored + pushed `d2cdbf7 state(nhtsa-recalls): stored efab48ed2da2 [skip ci]` (baseline 8cc4c537→efab48ed); firing 2 dedupe'd `unchanged` against it (no dup); `[skip ci]` triggered nothing; suite 8/8. One transitional dup per drifted collector (self-identifying), then clean. Evidence: buildlog 2026-07-28. Commits `d1b6178`+`6931ca4`.
**Decision (orchestrator, 2026-07-28):** **(a) per-collector state files.** (b) is rejected because shared-file commit races are *guaranteed* by our own over-scheduling doctrine (the shared `generated` line conflicts on every near-simultaneous pair); (c) is rejected because it leaves R1 non-self-persisting against SPEC-02 §1's letter and tolerates up to ~1 GB/month of pure complaints duplicates (weekly firing × monthly-ish upstream refresh). Locked design — do not re-litigate:
- **Source of truth:** `ops/state/health/<collector>.json`, one per collector (`ats-boards` is one). Distinct files ⇒ no merge conflicts by construction.
- **Legacy `HEALTH.json` becomes a compiled merged view** (keeps SPEC-02's letter + human readability): materialized by the weekly driver; readers (`opscore/report.py` `_collector_board`, `weekly.py` gate-filing) merge `ops/state/health/*.json` at read time with legacy fallback.
- **Framework:** `Collector`/`ats_boards` health path becomes per-collector in R1 (path param; verify mode unchanged).
- **Workflow:** `_collector.yml` gains `contents: write` + a state-commit step — `git pull --rebase --autostash`, retry ≤2, commit `state(<collector>): <action> <hash12> [skip ci]`, push; after 2 failed pushes exit nonzero loudly (job contract).
- **Keepalive folded in:** a trivial monthly `keepalive.yml` (`git commit --allow-empty` `[skip ci]`) closes the 60-day cron-disable backstop even through all-stable months.
- **Migration:** split the current `HEALTH.json` collectors node into the per-collector files in this session.
**Accept:** reader-merge + per-collector-write tests green; one real Actions firing **commits its state file**; the *next* firing of that collector dedupes `unchanged` against the freshly *committed* baseline (the proof W-002 couldn't produce); suite green; CI on the state commits stays quiet (`[skip ci]` honored).
**Catches:** push rejected repeatedly (race) → the rebase-retry is the mechanism, 2 retries then loud-fail; a state commit accidentally triggering workflows → fix the `[skip ci]` marker, never disable CI; anything tempting a shared-file shortcut → that is option (b), rejected above.
**Why:** R1 collector jobs run `contents: read` and never commit `ops/state/HEALTH.json`, so a source that has drifted from the *committed* baseline **re-stores identical content on every subsequent firing** until a session commits state (proven in W-002: nhtsa-recalls/complaints/ats-boards stored fresh vintages; their next firings would duplicate). SPEC-02 §1 expects collectors to commit state (*"a keepalive is unnecessary while collectors commit state"*). Bounded/non-destructive near-term (tiny board universe, weekly cadences, self-identifying same-hash duplicates), **but recurring 367 MB complaints duplicates would eventually breach R2's 10 GB free tier** → land this before heavy over-scheduling or C3 universe expansion.
*(The original three-option decision brief is preserved in the buildlog 2026-07-28 entry; option (a) was locked by the orchestrator — see the decision block above.)*

### W-003 · Alarms + weekly session live — `partial` (2026-07-28; code+session done, drill/scheduler blocked on operator infra)
**Result (2026-07-28):** Items **2, 5, 6 done + green**: `NTFY_*` persisted to User env (Actions secrets confirmed); one real weekly report `ops/reports/2026/W31.md` compiled + pulsed (ntfy path verified `200 OK`, operator confirms phone); futility-clause auto-gate wired into `run_weekly` (`maybe_file_futility_gate`, CALENDAR-sourced date, mandatory/re-files-on-inaction, `no-action` expiry invariant kept) + 4 tests. Items **1, 3, 4 mechanized/documented but operator-gated**: `opscore/healthchecks.py` + `ops/setup/healthchecks_setup.py` derive & create the 6 cron checks (grace = 1.5×max-firing-gap) and set `HC_<COLLECTOR>` secrets — **needs an HC API token** (⚑ #212, dry-run verified, +2 tests); `ops/playbooks/kill-drill.md` (`/fail`-primary, skips no collection window) — **runs after #212**; `ops/setup/schedule-weekly-session.ps1` registers the Mondays weekly session — **operator action** (permission-posture + subscription decisions) (⚑ #213). Plus SPEC-03 §1 forward-wiring: `HC_WEEKLY` weekly dead-man ping in `run_weekly` (inert until set). Suite 8/8 (opscore 19→25). **Residual for full acceptance:** SPEC-03 §6 drill pass (operator #212) + weekly job live (operator #213) — both operator-completable without a worker. Flagged: orphan clock nearing false-freeze (stale `ACK`, not forged); stale Vikunja dupes #9–13/#39–43/#84–88 need operator close. Evidence: buildlog 2026-07-28.
**Scope:** the watching layer stops being inert.
**Read:** `opscore/alarms.py`, `opscore/weekly.py`, `ops/SPEC-03`, `ops/playbooks/weekly-ops.md`.
**State inherited from W-002 (don't re-derive):** ntfy topic `theexhaust-75Z`, phone-confirmed; `NTFY_ALARM/GATE/PULSE` set as Actions secrets (all = that topic) but **needed in LOCAL env for the weekly session** (R2 runtime reads `os.environ`; `setx` persists for *fresh* processes only). healthchecks: one check exists — `HC_NHTSA_RECALLS` UUID `2b6e0c92-f34a-445c-83e8-6006c2d49fe8`, pinged from Actions; remaining checks may want a healthchecks.io API token (⚑ vtask if so). `_collector.yml` already exports every `HC_<COLLECTOR>` env — checks+secrets light them up with no code change. Working Python on the operator box: `C:\ProgramData\miniconda3\python.exe` (PATH `python` is the MS-Store shim). After W-002b, HEALTH readers merge `ops/state/health/*.json` — the weekly driver materializes the legacy view.
**Do:** healthchecks checks created per SPEC-03 §1 budget (≤18 collector/logical, grace = cadence × over-schedule); ntfy topics into local env + Actions secrets; kill-one-collector drill (SPEC-03 §6: disable a firing, confirm healthchecks→ntfy alarm within grace); schedule the weekly R2 session (Windows Task Scheduler → `claude -p` per SPEC-02 §2); run `python -m opscore.weekly` once for real and confirm the pulse lands on the phone; wire the futility-clause auto-gate (2027-12-31 from `CALENDAR.md`) into the weekly driver + a test.
**Accept:** SPEC-03 §6 drill items pass; one real weekly report compiled + pulsed; futility wiring tested offline.
**Catches:** ntfy delivery fails → topics are the only auth, regenerate + update secrets (operator ping via Vikunja if his action needed); healthchecks free-tier limits hit → group per §1 budget, never drop outcome-based pings.

## Phase B — the ground truths at scale

### W-004 · C2 WARN, tranche 1 (top-10 states) — `done` (2026-07-28, in commit at hand-off)
**Result (2026-07-28):** 10 states archiving to real R2 as one data-driven fleet (`collectors/warn.py` + `seed_warn.json`, mirrors ats-boards): **CA, TX, WA, IL, NJ** (structured xlsx/csv, parse clean) + **PA, FL, MD, WI, NY** (HTML; NY = retired/frozen table). Store-raw-always; stdlib-only best-effort parse into the manifest. Every source re-verified live with the collector's own `http_get` (200) on 2026-07-28. First firing stored all 10 (TX = 2,368 rows via `$limit`); second firing dedupe'd 6 (CA/TX/IL/NJ/PA/FL) + re-stored 4 volatile-HTML (NY/WA/MD/WI, ~127 MB/yr, within free tier). **Acceptance round-trip PASS:** a TX notice pulled back from R2 via `archive.theexhaust.org` (cloudflare), sha256 = manifest, real row shown (Amentum/Bowie/178/New Boston). `collect-warn.yml` + `_collector.yml` warn-branch + `HC_WARN`; healthchecks auto-derives the one `warn` check (grace 18h) → #212 now provisions 7. +8 warn tests; suite 9/9; covenant clean. **Deferred (gate `warn-tranche1-walled-sources`):** OH (JS-walled/headless), GA (no source/open-records), NY-current (Tableau) — swapped OH/GA→MD/WI to keep 10 live. **Candidate:** content-normalization pre-hash to restore dedupe on the 4 volatile-HTML sources. **Proven in Actions (post-hand-off):** dispatch `collect-warn.yml` run 30380851260 green in 40s — 5 stored + 5 dedupe'd vs the committed baseline, state committed back `state(warn): stored [skip ci]` (73b17dd). Evidence: buildlog 2026-07-28.
**Scope:** the WARN Watch corpus begins; heterogeneous per-state adapters.
**Read:** `ops/SPEC-01` C2 row, `docs/02-RESEARCH.md` §3-① WARN paragraph ONLY, `collectors/cms_deficiencies.py` as adapter pattern.
**Do:** per-state collectors for CA, NY, TX, WA, IL + 5 more by volume; **primary state sources only** (aggregators are cross-checks, not sources — covenant); per-state schema contracts (PDF states → store raw + parse what's parseable; parsing completeness is per-state metadata, not a gate); shared `warn` logical heartbeat.
**Accept:** 10 states archiving on schedule; ≥1 real notice visible end-to-end in a stored snapshot; suite green.
**Catches:** a state portal blocks datacenter IPs → 403 ladder step (b) operator box, log it; a state is JS-walled or CAPTCHA'd → STOP that state, gate item, continue the other nine (never burn a session on one state); format drifts mid-tranche → quarantine semantics already handle it.

### W-005 · Fleet-green + BUILD-01 acceptance — `partial` (2026-07-28; criteria 2–5 closed, criterion 1 time-bound to 2026-08-04)
**Result (2026-07-28):** SPEC-01 §6 **criteria 2–5 closed with evidence**; **criterion 1 (7 consecutive green days) pending time, not failing** — the whole fleet's archive clock started 2026-07-28, so the earliest satisfiable date is **2026-08-04**. Day 1: all 7 collectors green (0 quarantines / 0 pauses / 0 drift), one blemish on the record — `nhtsa-recalls` run 30366156694 `startup_failure` 13:59 (the W-002b callers-permission bug, fixed 5 min later) so its clean window starts 07-29. First *scheduled* firing observed: ats-boards cron `17:13` → fired **18:41 UTC (88 min drift)** — the doctrine's premise, live. **Drill PASS** (`ops/playbooks/drift_drill.py`, re-runnable, real cms-deficiencies collector vs a throwaway LocalFS root — live R2 untouched): 3× quarantine + alarm + heartbeat withheld + **`raw/` never polluted**, 3rd → `paused`+`needs_gate`, dup → no alarm storm, weekly filed exactly one `source` gate, recovery cleared the streak. **Covenant review PASS** (table in buildlog) — no violation; two *scale* obligations due at the C3 expansion gate (no inter-request delay in the fleet loops; the asserted ats-boards robots check is unlogged). **C7 Kroger dark CONFIRMED** (zero hits repo-wide) — note the guard does *not* enforce it (Kroger is gate-blocked, not registered). **Storage:** measured sweep 0.7889 GB / 49 objects → **$0.00/mo**; growth 21.0 GB/yr (91% = the nhtsa-complaints flat file, which changes daily so dedupe can't help) → $0.18/mo yr 1, $3.02 yr 10, **$5 bar at ~yr 16**. **Defects found + fixed:** ats-boards wrote **no per-day manifest** (live SPEC-01 §3 violation) and warn manifests carried no `git_ref` — both fixed, ref resolved once per fleet run, **proven live in Actions** (run 30393584449 → manifest in R2 with `git_ref 8eac509`), day-1 objects backfilled from the stored bytes and marked `backfilled`. Fleet-wide integrity check: **34/34 manifest sha256s match their object keys, 0 mismatch**. Tests +3 (engines 6, warn 9, opscore 26); suite 9/9. Evidence: buildlog 2026-07-28. Commit `8eac509` + hand-off.
**Residual (one command, no session):** `python ops/fleet_green.py` on/after **2026-08-04** → exit 0 = criterion 1 satisfied for all 7.
**Scope:** close BUILD-01 formally.
**Read:** `ops/SPEC-01` §6.
**Do:** verify 7 consecutive green days across enabled collectors (heartbeats + manifests); injected-drift drill; covenant review of every collector vs SPEC-01 §4; C7 Kroger confirmed dark; storage projection into `BUDGET.json` (< $5/mo bar).
**Accept:** SPEC-01 §6 checklist fully evidenced in the buildlog → orchestrator runs the **adversarial review** over all collectors added since the last pass **plus the workflow YAMLs (`_collector.yml` + callers + keepalive) and the W-002b state-commit machinery** → BUILD-01 marked accepted.
**Catch:** any collector <7 green days → BUILD-01 stays open for it; accept the rest, list the stragglers.
**Worker recommendation to the orchestrator (2026-07-28):** run the adversarial review **now** (scope also covers the WARN fleet/seed and today's manifest changes) and mark BUILD-01 accepted on **2026-08-04 conditional on `ops/fleet_green.py` exiting 0** — holding the review for a calendar wait buys nothing, and nothing but the clock is outstanding.

**Candidates filed by W-005** (orchestrator triages; none is a detour):
- **(a) inter-request rate-limit/jitter in the `ats_boards` + `warn` fleet loops** — no `time.sleep` exists anywhere; politeness is currently structural (1 host = 1 request/firing). Fine at 3 boards / 10 states; **must land before the C3 universe-expansion gate** (3–5k boards × 4 hosts).
- **(b) log the ats-boards robots/master-ToS check** its docstring asserts (no record exists) — same deadline as (a).
- **(c) mechanize C7 darkness** — a covenant-guard assertion that no `kroger*` collector exists until the ToS gate clears (today darkness is enforced by absence + a FLEET-roster test).
- **(d) content-normalization pre-hash for the 4 volatile-HTML WARN states** (inherited from W-004; measured cost is only ~32 MB/yr, so this is hygiene, not urgency).
- **(e) Node-20 deprecation** on `actions/checkout@v4` + `actions/setup-python@v5` across all workflows (annotation today, breakage later).

## Phase C — the first credibility artifact

### W-005b · Pre-launch placeholder page — `done` (2026-07-28, in commit at hand-off)
**Result (2026-07-28):** `sitegen.build --placeholder` emits exactly one `index.html` to `site/dist/` — identity + one-sentence lede, "Status: pre-launch. Nothing is published here yet — no numbers, no estimates, no claims", the factual "archive has been collecting since July 2026" line, and the method-before-results claim linking the public repo + the frozen NHTSA pre-registration ("check the git history yourself"). No nav (the other pages don't exist in this mode), same CSS spine, theme-aware, stdlib-only. **Full mode proven byte-identical** (all 5 pages hash-for-hash vs the pre-change build). **+3 tests (sitegen 2→5)**, two of them adversarial: nothing measured may appear (no bare number survives after ISO-dates/years/version tags are excluded; no `%`, PR-AUC, precision, scorecard, table, PASS/FAIL) and no tracker/third-party asset may load (`<script>`/`<img>`/`<iframe>`/`<link>`/`@import`/gtag/GA/plausible/fbq all barred; every external URL must be our own repo). **Caught a real publish hazard:** `site/dist/` is gitignored and *did* hold a full local build, so placeholder mode now **deletes the four unlaunched pages** from the output dir before writing — otherwise a deploy-from-dir would have quietly published held content; a test proves it. Both published links verified live (HTTP 200). Rendered page inspected, not just asserted. Suite 9/9. **Errand is #217, not #214** (NEXT.md carried a stale number — annotated #217 with the exact Pages settings + the wrangler fallback).
**Scope:** a single no-numbers placeholder on `theexhaust.org` ahead of the W-007 launch (operator's live decision 2026-07-28 — recorded in the constitution log; the full site stays held for the retrocast launch story). Placeholder build mode in `sitegen` + tests + deploy config documented for operator errand **#217** (Cloudflare Pages hookup).
**Scope:** a single no-numbers placeholder on `theexhaust.org` ahead of the W-007 launch (operator's live decision 2026-07-28 — recorded in the constitution log; the full site stays held for the retrocast launch story). Placeholder build mode in `sitegen` + tests + deploy config documented for operator errand **#214** (Cloudflare Pages hookup). Full spec in `NEXT.md` (the worker strips it at hand-off, leaving W-006 standing).
**Accept:** placeholder mode emits exactly one page (identity, pre-launch status, archive-since-July-2026 line, GitHub + pre-registration links; NO numbers/index content/named entities/trackers); full mode byte-identical; suite green; pushed. — **all met.**
**Deploy config (for #217):** Cloudflare Pages → Git integration, repo `mlawsonking/theexhaust`, production branch `main`, **build command `python3 -m sitegen.build --placeholder`**, **output directory `site/dist`**, root directory = repo root, custom domains `theexhaust.org` + `www`. `sitegen` is **stdlib-only**, so there is no install step and no `requirements.txt` to resolve; set build env `PYTHON_VERSION=3.13` if the image defaults lower. **Fallback if the Pages image can't run Python:** deploy from Actions with wrangler (`wrangler pages deploy site/dist`) after a build step — that route needs a `CLOUDFLARE_API_TOKEN` repo secret (Pages:Edit scope), which is an operator credential; #217 is annotated with both paths.

### W-005c · BUILD-01 review fixes — `done` (2026-07-28, commit `e29c326`)
**Result (2026-07-28):** **19/19 dispositioned — 17 fixed with regression tests, 2 deferred/accepted with reasons** in `docs/05-SCOPE-LEDGER.md` §5b (F16 SR pagination → C3 expansion gate, with truncated payloads now *refused* and SR blocked from the seed meanwhile; F19b two-writer manifest race → accepted: Actions is the only scheduled writer, the loss is an index entry never data, and the index is provably reconstructible). HIGH cluster: quarantine outcomes now reach committed state in both fleets (`last_action=quarantined`, `last_success` only on a clean run — previously `_collector.yml` skipped the commit and fleetgreen's quarantine leg was dead code); one dead board no longer kills the ats fleet; an empty-but-parseable board is a valid store (`postings=0`) rather than a 3×/day alarm that also dropped the vanished-postings snapshot; the covenant guard now scans `collectors/**/*.json` where every source URL has lived since W-004. Acceptance-evidence bugs both fixed (unreadable state ⇒ `STATE-UNREADABLE`, never vacuous GREEN; in-flight runs dropped, never false FAILED). Constitutional: the futility gate slug carries the armed date so a re-armed clause can fire, and the CALENDAR parse takes `max()` of valid dates. Plus 3-strike pause + `needs_gate` on both fleets, per-unit exception containment, **enforced** pause (paused ⇒ no fetch; only an operator decision resumes), warn volume detector, real 403-forensics (`http_get` returns non-2xx bodies), corrupt-state tolerance, empty-fleet ⇒ never ping green, `PARSER_VERSION`, polite rate-limit/jitter. **F11 covenant audit re-run live:** illinoisworknet robots = 404 (nothing disallowed); **fortress.wa.gov robots genuinely unverifiable** (ConnectionReset ×2) — recorded with its sanction basis rather than keeping the wrong citation. **Bonus bug found while fixing F05:** `gates.new_gate` wrote unsafe slugs into filenames — on Windows a colon creates an invisible NTFS alternate data stream, so an operator's gate silently vanished; it now raises, with a test. Suite 9/9, tests 26 → 61. **Proven live post-fix:** `collect-warn` run 30398988686 (10 states, 5 stored/5 dedupe'd, 0 quarantined, volume bands recorded, state committed `ec98b68` on push-retry 2 under real concurrency) and `collect-ats-boards` run 30398990995 (state `9db37a0`); drift drill re-run PASS against the new contract. Evidence: buildlog 2026-07-28.

### W-005c · BUILD-01 review fixes — (original order, for reference)
**Scope:** disposition all **19 confirmed findings** from the BUILD-01 adversarial review (`ops/state/REVIEW-BUILD01.md` is the spec: 4 HIGH — quarantine-state persistence, ats-fleet fetch containment, empty-board false-quarantine, covenant-guard seed-JSON hole; 8 MEDIUM incl. the futility re-arm defeat + both `fleet_green` evidence bugs; 7 LOW). Fix + regression-test, or dismiss/defer with reasons in the buildlog (F16/F17 may defer to the C3 expansion gate per the reviewer's own latency note). Hardening only — no new capabilities. Full order + priority in `NEXT.md` (worker strips at hand-off).
**Accept:** per-finding disposition table in the buildlog; named regression tests pass; suite green; one post-fix Actions firing green. **BUILD-01 acceptance (2026-08-04, #215) is blocked on this item + `fleet_green` exit 0.**

### W-006 · NHTSA retrocast: run → hostile review → publish gate — `done` (2026-07-29, commit `421a9bb`) · **retrocast FAILED its bars — published**
**Result:** the pre-registered retrocast ran against the archived vintages and **missed 3 of 4 frozen §7 bars** on the held-out 2021–2025 window; the 4th passed degenerately and is reported as not-evidence. PR-AUC **0.0280 vs volume-only 0.0331** (bar: beat volume by +0.05) · precision **0.0190** = the base rate (bar 0.30) · event-recall **0.4221**, which is also the *ceiling* (bar 0.50) · median lead 168 d with 1,646 of 3,295 leads sitting exactly at the 175-day window edge. **Cause is structural, not tunable: 57.8% of held-out recall campaigns occur in cells with no complaint at all in the preceding 26 weeks** (train coverage 0.3983, so not a test-window fluke), and at the MLE the two features the registration leaned on hardest — `rate_ratio` (−0.318) and `hazard_lang` (−0.150) — carry negative weight. No bar was moved, no model re-tuned. Ordering proven and asserted by the run itself: registration `e3d4d84` (2026-07-13) → workbook freeze `d28d8fa` → results code, clean tree. **Hostile review 6/6 zeroed, 5 findings** (`HOSTILE-REVIEW-v1.md`): leakage, vintage, base rate (scored universe 1.90% vs full grid 0.714% — disclosed, and it made the precision bar *easier*), dumb baselines, threshold archaeology, overclaim; under-training ruled out with |grad| = 5.99e-08 reproduced by an independent IRLS solve. Two pre-publication corrections disclosed with pre-fix numbers (labels are distinct events not flat-file rows: event-recall 0.3120 → 0.4221, still failing; `dirty` provenance). Autopsy in `retrocast/DEAD-REGISTRATIONS.md`. **The ⚑ operator launch gate was not reached** — a failed retrocast opens no named tier — but the decision it implies is filed: gate `GATE-20260729-nhtsa-v1-dead-next-move` + ⚑ **#219**. Suite 11/11 (+21 tests). Evidence: buildlog 2026-07-29.

### W-006 · (original order, for reference)
**Scope:** the flagship. Run the pre-registered retrocast against archived vintages; produce results v1.
**Read:** `retrocast/nhtsa-recalls/PRE-REGISTRATION-v1.md` (the law), `retrocast/harness.py`, `ops/SPEC-08` §3/§5.
**Do:** signal construction per the frozen spec §3 (deterministic; hazard lexicon frozen in the workbook first); run harness on archived complaints/recalls vintages (retrocast-of-record, never live endpoints); emit `results/v1/` + `scorecard.json` citing the registration commit; write `REPORT.md`; then a SEPARATE hostile-review session walks SPEC-08 §5 (leakage, vintage, base-rate, dumb-baseline, threshold archaeology, overclaim) to zero.
**Accept:** scorecard validates; registration commit demonstrably predates results; hostile checklist zeroed; **then the ⚑ operator launch gate** (TX LLC + insurance decision + sign-off — gets `vtask add` when reached).
**Catches:** **bars fail → that is a publishable outcome**: dead-registration autopsy + a v2 pre-registration if a fixable flaw is found (disclosure mandatory) — the clause exists for exactly this; compute too heavy for Actions → operator box (the 4080 is idle); component-taxonomy mismatch vs layout doc → freeze the mapping in the workbook, note it, never bend the spec silently.

### W-007 · BUILD-04 launch surfaces — `partial` ⚑ (2026-07-29; surfaces done, both residuals are operator gates)
**Scope:** WARN Watch + posting-diff pages + feeds + artifact compiler + deploy.
**Read:** `sitegen/build.py`, `engines/posting_diff.py`, `resolver/receipts.py`, `ops/SPEC-04` autonomous table, gameplan §6 BUILD-04.
**Do:** artifact compiler (cadence + anomaly artifacts from approved templates, **fail-closed through `receipts.has_valid_bundle`** — an unreceipted number must refuse to render, with a test proving it); WARN Watch page + per-state pages from archived snapshots; posting-diff pages from the ATS fleet; RSS/JSON feeds; stale-data banners wired to HEALTH; Cloudflare Pages deploy of `site/dist`; Bluesky posting dark until ⚑ handle exists.
**Accept:** site live on Pages; a WARN notice flows source→archive→page→feed with receipts within one collector cycle; two weeks unattended is the BUILD-04 bar (tracked by the weekly reports, not by a session).
**Catches:** Pages build quirks → build in Actions, deploy artifact only; a page needs a number lacking a bundle → that's the fail-closed system WORKING — build the bundle path, never bypass.
**Result:** `artifacts/{extract,templates,compile}.py` + `sitegen/{build,feeds}.py`: 36 pages from the live archive (WARN Watch + 10 state pages, Postings + 3 board pages, 14 receipt pages, RSS + JSON Feed, HEALTH-wired stale banners, methodology `#warn-watch`/`#posting-diff`). **8 of 10 WARN states extract into individual notices; PA/WI are labelled "archived, not yet machine-readable" rather than given a derived count.** Fail-closed proven both ways against real data (stripped bundle + deleted bundle → `UnreceiptedNumber`, nothing written). **Acceptance flow traced on a real filing** — PD Systems / Monterey County / 81 workers, between two CA vintages 44 min apart → page → both feeds → receipt carrying both input hashes. Six defects found and fixed with tests (reshaped-source-as-mass-layoffs circuit breaker; NJ future-dated recency ranking; NY dedupe undercount 61→69; truncated legal name; covenant guard blind to `artifacts`/`sitegen`; home-page retrocast overclaim). Suite **12/12, tests 61 → 87**. Evidence: buildlog 2026-07-29.
**Residual (operator, not a worker):** ⚑ **#217** Pages hookup — `.github/workflows/site.yml` is written and inert: `workflow_dispatch` only (**no cron** — a scheduled full publish would decide #219 by default), `mode` defaults to the approved `placeholder`, deploy step fails loudly naming #217 and uploads `site/dist` as a run artifact instead. ⚑ **#219** owns whether the full site publishes at all; the FAIL scorecard was not softened or hidden.
**WORKPLAN candidates raised (not worked):** (a) PA/WI/OH/GA notice extraction (link lists + documents — needs a per-state parser, or the deferred `warn-tranche1-walled-sources` gate); (b) the WARN collector's `parsed_rows` counts raw `<row>` tags, so IL reports **408** where the sheet holds **16** real notices — the volume-anomaly detector is banding on the wrong quantity for spacer-padded workbooks; (c) W-001's Cloudflare Bot-Fight-Mode finding (bare `Python-urllib` 403s; framework `DEFAULT_UA` unaffected) remains latent and unactioned.

### W-007b · `cms-pbj` collector — the missing half of SPEC-01 C1 — `done` (2026-07-29)
**Result:** `collectors/cms_pbj.py` + `collect-cms-pbj.yml` + `entry: pbj`. **Source re-verified live and research §5 pointed at the wrong catalog** — the raw PBJ files are absent from `data.cms.gov/provider-data` (staffing *ratings* only) and live in the main DCAT catalog `data.cms.gov/data.json`: `Payroll Based Journal Daily Nurse Staffing` (`7e0d53ba-…`), quarterly, **37 releases 2017Q1…2026Q1**, 33 columns, `PROVNUM` = the CCN joining to `cms-deficiencies`. Built as a **fleet** (the catch fired: one file per quarter, all retained ⇒ each release keeps its own prefix + manifest entry, never concatenated), so registration is the `_collector.yml` entry branch, not `run.py`. Quarter identity resolves across three filename generations + opaque-id releases, agreeing on **37/37, zero duplicates**. Politeness: change detected from the per-release URL + HEAD `Last-Modified` — **49 s to store, 2.3 s to dedupe** on a 234 MB file. **Defect caught by its own test:** absence of a `Last-Modified` was treated as "unchanged" and skipped the download, which would have silently lost an in-place revision — the very thing SPEC-01 C1's "CMS overwrites revisions" warns about; the skip now needs positive evidence of sameness. **Proven, not asserted:** 2026Q1 (1,303,830 rows / 234 MB → 30.4 MB) verified from R2 bytes + byte-identical over `archive.theexhaust.org`; **2025Q4 stored BY ACTIONS** with state committed back (`90c39f8`); dedupe proven against the committed baseline locally and in Actions. Backfill is opt-in (`--all`), **measured at 8.7 GB raw → ~1.1 GB stored**; `published_releases` vs `release_count` exposes the un-backfilled gap in state. Roster: **`fleetgreen.FLEET` → 8** (⚑ #215 now covers it; its Actions runs are already green so the 2026-08-04 date is not pushed) and **healthchecks auto-derives an 8th check `HC_CMS_PBJ`** for ⚑ #212. Runner hardened: an unmapped fleet `entry` now fails loudly instead of writing `ops/state/health/.json` and never committing state. Suite **13/13, +14 tests**. Evidence: buildlog 2026-07-29.
**Candidate noted, not worked:** the shared state-commit message renders a blank hash for fleet collectors (fleets keep hashes per unit, not at the node) — cosmetic, pre-existing, shared with `warn`/`ats-boards`.

*Original order (retained for the reasoning that re-pointed the queue):*
**Why this jumped the queue:** SPEC-01 §2 **C1 is `cms-pbj` + `cms-deficiencies`**. Only `cms-deficiencies` was ever built — `collectors/run.py`'s registry has no PBJ entry and no PBJ object exists in R2. Three things follow: (1) **BUILD-01's first-priority collector is half-delivered**, which the orchestrator should know before closing ⚑ #215 (it does *not* break SPEC-01 §6 criterion 1 — an unbuilt collector is not an un-green *enabled* one — so this is a scope question, not a green-days question); (2) **W-008's trigger "≥2 PBJ vintages archived" can never fire**, so the next retrocast is unstartable as queued; (3) SPEC-01 marks PBJ **"CMS overwrites revisions"**, so this is perishable under the collect-before-you-can-compute doctrine — each uncollected release is a vintage nobody can recover.
**Scope:** one collector, on the already-approved roster (C1 — **no new-source gate needed**, and no covenant question: same publisher, same CMS open-data posture as `cms-deficiencies`).
**Read:** `collectors/cms_deficiencies.py` (the closest sibling — Socrata resolve + `CsvSchema` + `Collector`), `collectors/run.py` (registry), `ops/SPEC-01` §2 C1/§3/§5, `.github/workflows/collect-cms-deficiencies.yml` (copy the caller), research §5 row "CMS PBJ staffing".
**Do:** re-verify the PBJ endpoint live **before depending on it** (standing order — research last checked it 2026-07-11; if the dataset id or shape has drifted, STOP and file a gate rather than improvise); build the collector against the real payload with a schema contract that pins the columns the retrocast needs (CCN key + staffing hours + reporting quarter); register it; add the scheduled caller (quarterly release + weekly probe per C1 cadence); add `HC_CMS_PBJ` to `_collector.yml` and note it for ⚑ #212's provisioning list (**7 → 8 checks**); archive one real vintage to R2 and verify the object + manifest; tests alongside.
**Accept:** one live vintage stored in R2 with a per-day manifest carrying `git_ref` and schema version; a second firing dedupes `unchanged`; schema validation proven against the real file; suite green.
**Catches:** endpoint moved or shape drifted → **STOP + gate**, do not hunt for a substitute dataset; payload is large → the same read-cap/streaming pattern `nhtsa-complaints` already uses (368 MB works today); PBJ publishes as multiple files per release → archive each as its own manifest entry rather than concatenating (the retrocast needs the release boundary intact).

## Phase D — scheduled expansions (pull forward only via SCOPE-LEDGER triggers)

### W-007c · BUILD-04 review fixes + NHTSA artifact corrections — **`done` 2026-07-30 — 21/21 dispositioned, all fixed**
**Outcome:** every one of the 21 confirmed findings is fixed with a regression test or a committed artifact correction; **no bar moved, no metric changed, no retrocast was re-run, nothing was deployed.** The through-line: the publish path was fail-closed on a number with no receipt bundle and fail-**open** on every way the layer underneath could break — a corrupt `artifacts.json` read as "no artifacts", a corrupt `scorecard.json` as "no scorecard", a damaged health file as "no health state". Now one rule, **absent is a state, broken is a refusal**, expressed as a `RefusedBuild` family that aborts the build. Suite **15/15, +18 tests** (sitegen 13→26, cms-pbj 14→18, opscore 30→31). Refusals proven against a copy of live repo state: corrupt scorecard → `CorruptScorecard`; deleted/junk `artifacts.json` → `DerivedLayerError`; a claim inflated 10× against its own bundle → `UnreceiptedNumber`; each leaves no half-built site. ⚑ **#215 unaffected and on track** — `fleet_green` still 7/8 GREEN with `nhtsa-recalls`'s 07-28 `startup_failure` the sole laggard, ageing out before 08-04; no collector carries a paused unit, so the G06 fix is additive. Storage re-projected from a **real** sweep (new `ops/storage_sweep.py`): 2.1766 GB / 160 objects, `raw/cms-pbj` now the second line. Full disposition table: buildlog W-007c.
**Not worked, deliberately:** ⚑ #219 was not pre-empted (`site.yml` still cron-free and `placeholder`-default), and neither FAIL scorecard was softened, moved, or hidden — both now carry *more* disclosure than before.

#### Candidates raised by W-007c (recorded, not worked)
- **Hospital/Care carries the identical G20 defect.** `hospital-care/HOSTILE-REVIEW-v1.md` §5 and `DEAD-REGISTRATIONS.md` assert an independent Newton/IRLS solve that `hospital_care/run_v1.py` does not emit. Out of this item's stated scope (NHTSA artifact corrections), and the independent hostile confirmation of that failure is separately owed to the orchestrator — so raised, not quietly fixed on a sibling artifact.
- **A recovery still cannot reach committed state** (W-008's candidate, now load-bearing): G10's manifest-authority path deliberately reports `unchanged` on a rerun after a lost ledger, and `_collector.yml` skips the state commit on `unchanged` — so a recovered baseline lands in the runner and not in `main`. Mirror of W-005c/F02; `_collector.yml` is shared by all 8 collectors.
- **The gate slug names the wrong cause.** `_fleet_gate` emits `cms-pbj-fetch-3x-<Q>`, but after G05 a *drift* pause reaches it too, handing the operator a filename that says "fetch" about a schema problem.

### W-008 · Hospital/Care retrocast (BUILD-05) — **`done` 2026-07-30 — RAN and FAILED its pre-registered bars; published with an autopsy**
**Outcome:** 3 of 4 bars failed on the held-out window (369,750 cell-weeks, 4,643 harm events, 14,314 facilities). PR-AUC **0.1771 vs the prior-harm baseline's 0.2526** (bar +0.05) · precision **0.1794** vs base rate 0.1357 (bar 0.35) · event-recall **0.4605**, ceiling 0.9468 (bar 0.50) · median lead **154 d**, 43.3% at the edge — the one bar passed, and the pre-committed degeneracy rule legitimately did not fire. **Cause of death: a facility's own citation history out-ranks any staffing measure**, and the nine-feature signature beats plain staffing level by +0.0045. Registration `d6b78c3` (pushed, asserted an ancestor of HEAD and of origin/main) → results `ecb1de7`, clean tree. Hostile review **6/6 zeroed, 5 findings** — including a **real defect in the shared harness**: the planted-leak guard did not catch a label oracle (fixed, both plant shapes now regression-tested). Suite **15/15, +30 tests**. Full record: buildlog W-008 · [REPORT](../../retrocast/hospital-care/REPORT.md) · [HOSTILE-REVIEW](../../retrocast/hospital-care/HOSTILE-REVIEW-v1.md) · [autopsy](../../retrocast/DEAD-REGISTRATIONS.md).
**Backfill:** all 37 PBJ releases fetched — 26 in `raw/`, **11 quarantined on a legacy lowercase header** (2017Q1–Q4, 2018Q4, 2019Q1–Q4, 2020Q2–Q3), bytes preserved under `quarantine/cms-pbj/`. Archive 1.9 GB of a 10 GB free tier.
**Not worked, deliberately:** the gameplan §6 **county-level care-fragility page**. W-008's acceptance is the retrocast, and a "care-fragility index" published off a failed retrocast is the overclaim the gate exists to prevent. Severable → candidate below.
**⚑ Orchestrator, two items:** (a) **this item was worked out of order.** The queue was correct — `NEXT.md` was re-pointed to W-007c ONLY in `d986118` — but the session read it **without fetching first** and acted on a stale checkout. W-007c is untouched and is now `NEXT.md`'s only item. Recommended: make `git fetch && git pull --rebase` a required first step in BUILD-PROTOCOL §2. (b) as with W-006, the **independent hostile-review confirmation** is still owed — this one was written in-session.

#### Candidates raised by W-008 (recorded, not worked)
- **PBJ legacy-header releases (11 quarters).** Case-insensitive column matching plus an alias map (`hrs_rn_donadmin` → `Hrs_RNDON`+`Hrs_RNadmin`, `hrs_lpn_admin`, `hrs_na_trn`) would recover 2017–2020 from the already-archived quarantine objects with no refetch. Unblocks a v2 with a much longer signal history. Touches `CsvSchema`, shared by 8 collectors.
- **A recovery cannot reach committed state** (mirror of W-005c/F02). `_collector.yml` skips the state commit whenever the action is `unchanged`, so a collector whose node `last_action` is `quarantined` can only be cleared by a run that *stores* — for a quarterly source that is months. Bit ⚑ #215 this session; cleared by a real `--quarters 1` run, not a hand edit.
- **`release_count` no longer signals the gap.** It reads 37/37 published while 11 are quarantined, not archived — the exact "a retrocast cannot assume history it does not have" warning W-007b built it for. Count `raw/` releases, or expose `stored_releases`.
- **County-level observational staffing surface** (severable from the failed retrocast): PBJ carries `COUNTY_NAME`/`COUNTY_FIPS`, so county mean HPRD and its quarter-over-quarter change are publishable observational facts with receipts under covenant 2, with no signature claim.
- **v2 hypotheses**, none tested and none claimed: facility-*quarter* unit rather than facility-week; prior-harm rate as a **covariate** rather than a rival; CMS case-mix adjustment; a horizon matched to the ~110-day median inter-survey gap; and a re-run against the deeper label history the archive accumulates.

### W-008 (original order, for reference) — trigger **FIRED 2026-07-29** (2 PBJ vintages archived; W-007b done)
**Scope:** the second retrocast — PBJ nurse staffing → subsequent CMS harm deficiencies, hard-keyed on CCN. Research §5 calls it the cleanest in the portfolio: no semantic join, leak-free by construction, 418k dated ground-truth citations.
**Read:** `ops/SPEC-08` §3/§5/§7, `retrocast/harness.py`, `retrocast/nhtsa-recalls/PRE-REGISTRATION-v1.md` (as the *form* a registration takes — not its content), `collectors/cms_pbj.py` + `collectors/cms_deficiencies.py` (what the archive actually holds), gameplan §6 BUILD-05.
**Do:** (1) **run the history backfill first** — `python -m collectors.cms_pbj --all` (or dispatch `collect-cms-pbj.yml` with `--all`), ~8.7 GB raw → **~1.1 GB stored, measured**; two quarters satisfy the trigger's letter but cannot support a retrocast. Same for the deficiencies side: check what vintages exist and whether history is reachable. (2) **Pre-register before computing anything** — signal, labels, controls, splits and pass/fail bars frozen and committed, ordering asserted by the run. (3) Run on archived vintages only, hash-pinned. (4) `results/v1/` + `scorecard.json` + `REPORT.md`. (5) A SEPARATE hostile-review pass walks SPEC-08 §5 to zero.
**Accept:** same bar as BUILD-03 — registration commit demonstrably predates results, scorecard validates, hostile checklist zeroed, dumb baselines published.
**Catches:** **bars fail → publish the failure** with an autopsy (`DEAD-REGISTRATIONS.md`), exactly as W-006 did; the deficiencies file is overwritten in place, so if the needed history is not in the archive **say so and scope the retrocast to what is**, never reconstruct from a live endpoint; CCN reuse/closure across years is a real join hazard — freeze the handling in the workbook, don't bend it silently; compute too heavy for Actions → the operator box.
### W-009 · Expansion collectors C6/C8/C10/C11 — `queued` — model-bills (Wayback-only for ALEC-Exposed), EDGAR 8-K (10 req/s + UA rule), mouseprint, EIA-861
### W-010 · Workbook compiler (BUILD-06) — `queued` — trigger: two indexes live
### W-011 · Leg-authorship + FOIA micro (BUILD-07) — `queued` — **calendar-armed: prep gate fires Nov 2026** (statehouse session Jan–Apr 2027; E2 ports from OnScript)
### W-012 · Grocery pilot + Say-Do pilot (BUILD-08/09) — `queued` — Q2 2027; Kroger **⚑ human ToS read is the hard gate**, fallback = alt retailer APIs or shrinkflation-only
### W-013 · Track Record v1 + bank aggregate + first 311 city (BUILD-10) — `queued` — Q3 2027

*Everything else ideated lives in [`docs/05-SCOPE-LEDGER.md`](../../docs/05-SCOPE-LEDGER.md) with explicit triggers — nothing enters this plan except through a trigger firing or an operator gate.*
