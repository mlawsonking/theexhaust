# The Exhaust entity resolver + receipts store (SPEC-09) — the semantic-join moat and the
# immutable trail from every published number back to raw exhaust. Tiered (T0 hard keys -> T1
# deterministic crosswalks -> T2 normalized-name similarity -> T3 gated LLM), ledgered, and itself
# retrocast-scored. Stdlib-only; T3 is gated (never in R1). Engines call this; they never ship
# their own matching.
