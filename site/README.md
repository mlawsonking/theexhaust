# site/

The public static site: per-index pages, per-place pages, the Track Record scoreboard, methodology pages, receipts links, embeds/feeds. Static-generated (Python, no local Node — proven OnScript pattern), deployed via GitHub → Vercel.

Empty until Phase 4.
